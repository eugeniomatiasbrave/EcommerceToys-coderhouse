

<div class="shadow-lg bg-white max-w-xl mx-auto mt-32 rounded-xl w-2/3">
        <div class="card-body w-full transition-all duration-300">
            <h2 class="text-2xl font-bold mb-6 text-blue-800" >Actualización del perfil</h2>
           <form id="formProduct" action="/api/users/profile" method="POST" class="space-y-4" enctype="multipart/form-data">
    <input type="hidden" name="userId" value="{{user.id}}">
    <div class="form-control">
        <label for="firstName" class="block text-sm/6 font-medium text-gray-900">👤 Nombre</label>
        <input name="firstName" id="firstName" required value="{{user.firstName}}" class="block w-full border border-gray-300 rounded-md bg-white px-3 py-1.5 text-base text-gray-900">
    </div>
    <div class="form-control">
        <label for="lastName" class="block text-sm/6 font-medium text-gray-900">👤 Apellido</label>
        <input name="lastName" id="lastName" required value="{{user.lastName}}" class="block w-full border border-gray-300 rounded-md bg-white px-3 py-1.5 text-base text-gray-900">
    </div>
    <div class="form-control">
        <label for="email" class="block text-sm/6 font-medium text-gray-900">📩 Email</label>
        <input name="email" id="email" type="email" required value="{{user.email}}" class="block w-full border border-gray-300 rounded-md bg-white px-3 py-1.5 text-base text-gray-900">
    </div>
    <div class="form-control">
        <label for="birthDate" class="block text-sm/6 font-medium text-gray-900">Fecha de nacimiento</label>
        <input type="date" name="birthDate" id="birthDate" required value="{{user.birthDate}}" class="block w-full border border-gray-300 rounded-md bg-white px-3 py-1.5 text-base text-gray-900">
    </div>
    <div class="form-control mt-6">
        <input type="submit" value="Actualizar Perfil" class="w-full rounded-md bg-blue-700 px-3 py-2.5 text-sm font-semibold text-white">
    </div>
</form>
        </div>
        <div class="text-center text-gray-500 pb-4">
          <p>Por favor, asegúrate de que tu información es correcta antes de actualizar tu perfil.</p>
        </div>
    </div>

    <script src="/js/profile.js"></script>