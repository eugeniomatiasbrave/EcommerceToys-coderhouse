
<div class="container text-gray-800 mx-auto p-6 rounded-lg shadow-lg mt-10 w-1/3">
  <h1 class="text-4xl font-bold text-blue-800 text-center mb-8">🎟️ Detalle del Ticket</h1>
  <p><strong>Código del Ticket:</strong> {{ticket.code}}</p>
  <p><strong>Fecha de Compra:</strong> {{ticket.purchase_datetime}}</p>
  <p><strong>Comprador:</strong> {{ticket.purchaser}}</p>
  <h3 class="text-2xl font-bold mb-4">🛒 Productos Comprados</h3>
  <table class="table-auto text-gray-800 w-full border-collapse border border-gray-400">
    <thead>
      <tr>
        <th>Producto</th>
        <th>Cantidad</th>
        <th>Precio Unitario</th>
        <th>Subtotal</th>
      </tr>
    </thead>
    <tbody>
      {{#each ticket.products}}
      <tr>
        <td>{{this.product.title}}</td>
        <td>{{this.quantity}}</td>
        <td>${{this.product.price}}</td>
        <td>${{multiply this.quantity this.product.price}}</td>
      </tr>
      {{/each}}
    </tbody>
  </table>
  <p class="my-2"><strong>Total:</strong> ${{ticket.amount}}</p>

<div class="m-4 flex justify-end">
    <a href="/" class=" rounded font-semibold bg-blue-700 text-white hover:text-white p-2 ">Volver a inicio</a>
</div>

</div>