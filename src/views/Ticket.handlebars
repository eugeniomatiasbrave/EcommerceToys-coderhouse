
<div class="container mx-auto p-6 rounded-lg shadow-lg mt-10 w-1/2">
    <h1 class="text-4xl font-bold text-blue-800 text-center mb-8">🎟️ Detalle del Ticket</h1>
    <div class="card shadow-lg compact bg-base-50 w-full border border-gray-300 p-6 text-gray-800">
        <div class="card-body">
            <div class="mb-6">
                <h2 class="card-title text-xl font-semibold">Código del Ticket: <span class="text-blue-600">{{ticket.code}}</span></h2>
                <p><strong>Fecha de Compra:</strong> {{ticket.purchase_datetime}}</p>
                <p><strong>Comprador:</strong> {{ticket.purchaser}}</p>
            </div>
            <h3 class="text-2xl font-bold mb-4">🛒 Productos Comprados</h3>
            <table class="table-auto w-full border-collapse border border-gray-300 text-gray-800">
                <thead>
                    <tr class="bg-gray-100">
                        <th class="border border-gray-300 px-4 py-2 text-left">Producto</th>
                        <th class="border border-gray-300 px-4 py-2 text-center">Cantidad</th>
                        <th class="border border-gray-300 px-4 py-2 text-right">Precio Unitario</th>
                        <th class="border border-gray-300 px-4 py-2 text-right">Subtotal</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each ticket.products}}
                    <tr>
                        <td class="border border-gray-300 px-4 py-2">{{this.product.title}}</td>
                        <td class="border border-gray-300 px-4 py-2 text-center">{{this.quantity}}</td>
                        <td class="border border-gray-300 px-4 py-2 text-right">${{this.product.price}}</td>
                        <td class="border border-gray-300 px-4 py-2 text-right">${{multiply this.quantity this.product.price}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
            <div class="mt-6 text-right">
                <p class="text-lg font-bold">💵 Total: ${{ticket.amount}}</p>
            </div>
        </div>
    </div>
</div>